<% if @event.destroy %>
  $("#<%= dom_id(@event) %>" ).remove();
  toastr.success('Event deleted successfully.');
<% else %>
  toastr.error("<%= @event.errors.full_messages.to_sentence %>");
<% end %>
